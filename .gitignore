# build artifacts
/bin/
/dist/

# go
/vendor/
*.test
*.out
coverage.txt

# editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# local
*.local
.env.local
# NOTE: `.env` itself is NOT ignored — it's the cascading folder-config file
# that mus reads (replaces the old TOML `.mus` design). Users typically
# commit project/experiment-level `.env` files.

# user-side data dumps that should never be tracked
*.ndjson
all_gbiomed_mango_data.json
/tmp/
